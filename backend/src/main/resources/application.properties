# ═══════════════════════════════════════════════════════════════
# Community Digital Banking Platform – Application Configuration
# ═══════════════════════════════════════════════════════════════

# ── Application ──
spring.application.name=community-digital-banking

# ── MySQL Database (Aiven Cloud) ──
spring.datasource.url=jdbc:mysql://mysql-3d7069fb-ashrithreddy1825-0c04.f.aivencloud.com:20727/defaultdb?sslMode=REQUIRED
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ── SQL Init (set to 'always' if you need to re-create tables from schema.sql) ──
spring.sql.init.mode=never

# ── Server ──
server.port=8080

# ── JWT Configuration ──
jwt.secret=d2FsbGV0LXBsYXRmb3JtLXNlY3VyZS1rZXktZm9yLWp3dC10b2tlbi1nZW5lcmF0aW9uLTIwMjY=
jwt.expiration_ms=86400000

# ── Logging ──
logging.level.com.banking.platform=INFO
logging.level.org.springframework.security=WARN